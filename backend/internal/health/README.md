# internal/health

Health check endpoint. `GET /health` returns the current timestamp and a `"healthy"` status.

This package is a reference implementation of the `Handler[Req, Res]` interface pattern.
