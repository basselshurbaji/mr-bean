# db/sqlc

Auto-generated by sqlc. Do not edit any file in this directory by hand.

To regenerate:

```bash
$(go env GOPATH)/bin/sqlc generate
```

Configuration lives in `backend/sqlc.yaml`. Type overrides are set there (e.g. `uuid → string`, `timestamptz → time.Time`).
